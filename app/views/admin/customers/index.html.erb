<h1>ユーザー一覧</h1>

<% @customers.each do |customer| %>

  <%= customer.name %>

  <%= customer.email %>

  <% if customer.is_deleted? %>
    <th>退会</th>
  <% else %>
    <th>有効</th>
  <% end %>

<% end %>